receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  logging:
  otlp/jaeger:
{{ range nomadService "jaeger-backend-gRPC2" }}
    endpoint: {{ .Address }}:{{ .Port }}
{{ end }}
    tls:
      insecure: true

processors:
  batch:

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging, otlp/jaeger]
